export const buildingslistSelector = state => state.buildings.list;
export const buildingsLoadingSelector = state => state.buildings.loading;
export const buildingsCountSelector = state => state.buildings.count;
